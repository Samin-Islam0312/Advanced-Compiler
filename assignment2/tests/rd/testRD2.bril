# ARGS: rd
@main {
.entry:
  x: int = const 0;
  c: bool = gt x x;
  br c .then .else;
.then:
  x: int = const 1;
  jmp .join;
.else:
  x: int = const 2;
  jmp .join;
.join:
  y: int = add x x;          # two reaching defs to x at join
  print y;
}

